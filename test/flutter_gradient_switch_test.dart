import 'package:flutter_test/flutter_test.dart';

import 'package:flutter_gradient_switch/flutter_gradient_switch.dart';

void main() {
  test('adds one to input values', () {
    final calculator = GradientSwitch();
  });
}
